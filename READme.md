install all the given libraries
make a model named listing and initialize data from init folder
run the app.js file



middleware post for deleting the reveiws on deletion of listing is in "/models/listing.js"